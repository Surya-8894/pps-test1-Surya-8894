#include <stdio.h>
#include <math.h>
int main()
{
  
  float n , squareroot;
  printf("Enter the number:");
  scanf("%f",&n);
  squareroot = sqrt(n);
  printf("square root of %f is %f",n,squareroot);
  return 0;
  
}
